{
  "name": "pulse-energy",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "build": "rm -rf ./build/ && tsc && cp -R ./Files ./build/",
    "start-server": "node build/server/server.js",
    "start-client": "node build/client/client.js",
    "start-sendData": "node build/client/sendExcelData.js",
    "dev-server": "ts-node-dev --respawn --pretty --transpile-only server/server.ts",
    "dev-client": "ts-node-dev --respawn --pretty --transpile-only client/client.ts",
    "dev-sendData": "ts-node-dev --respawn --pretty --transpile-only client/sendExcelData.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Satyam Anand",
  "license": "ISC",
  "keywords": [],
  "description": "",
  "dependencies": {
    "csv-parse": "^5.5.3",
    "dotenv": "^16.3.1",
    "typescript": "^5.3.3",
    "ws": "^8.16.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@types/ws": "^8.5.10",
    "nodemon": "^3.0.2",
    "ts-node-dev": "^2.0.0"
  }
}
